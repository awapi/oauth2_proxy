# prepare environment - personal usage only 

- install dep
```shell
sudo apt-get install go-dep
```

- install golangci-lint which replaced gometalinter
```shell
go get -u github.com/golangci/golangci-lint/cmd/golangci-lint
```

